name: Patch Operational Systems evidence gate

on:
  push:
    branches: [agent/os-evidence-gate-2026-08-13]

permissions:
  contents: write

jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply focused gate repair
        run: python scripts/patch-os-evidence-gate.py

      - name: Validate harness and static regression
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          text = Path('operational-systems-acceptance-harness.html').read_text()
          match = re.search(r'<script>\s*(.*?)\s*</script>\s*</body>', text, re.S)
          if not match:
              raise SystemExit('Could not extract harness JavaScript')
          Path('/tmp/os-acceptance-harness.mjs').write_text(match.group(1))
          PY
          node --check /tmp/os-acceptance-harness.mjs
          python scripts/validate_os_output_integrity.py
          git diff --check

      - name: Commit focused repair
        run: |
          if git diff --quiet -- operational-systems-acceptance-harness.html scripts/validate_os_output_integrity.py; then
            echo 'Evidence gate already patched.'
            exit 0
          fi
          git config user.name 'Monderman-Group'
          git config user.email 'jason.ivan.adamson@gmail.com'
          git add operational-systems-acceptance-harness.html scripts/validate_os_output_integrity.py
          git commit -m 'Extend OS evidence-discipline acceptance gate'
          git push origin HEAD
