name: Frontend release gate
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Correct temporary public beta patcher
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        run: python scripts/fix_public_beta_workspace_patch.py
      - name: Correct public beta release validator
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        run: python scripts/fix_public_beta_validator.py
      - name: Apply public beta hardening on release branch
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        run: python scripts/public_beta_hardening_20260819.py
      - name: Add final verified disclosures
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        run: python scripts/finalize_public_beta_disclosures.py
      - run: python scripts/validate_frontend_release.py
      - name: Run diagnostic inline validation
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        run: python scripts/validate_diagnostic_inline_js.py
      - name: Commit generated release files
        if: github.event_name == 'pull_request' && github.head_ref == 'agent/public-beta-hardening-20260819'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'Monderman-Group'
          git config user.email 'jason.ivan.adamson@gmail.com'
          git add privacy.html security.html pattern-trial.html plan-pattern.html workspace.html workspace-diagnostics.html workspace-analysis.html workspace-actions.html workspace-settings.html scripts/validate_frontend_release.py
          if git diff --cached --quiet; then
            echo 'No generated changes to commit.'
            exit 0
          fi
          git commit -m 'Finalize public beta disclosures and release contracts'
          git push origin HEAD:${GITHUB_HEAD_REF}
