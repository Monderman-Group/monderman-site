name: Meta-synthesis frontend customer-release regression

on:
  push:
    branches: [main]
    paths:
      - 'monderman-report.js'
      - 'workspace-analysis.html'
      - 'workspace-diagnostics.html'
      - 'workspace.html'
      - 'workspace-actions.html'
      - 'workspace-assistant.js'
      - 'diagnostics.html'
      - 'cross-tool-synthesis.html'
      - 'scripts/validate_meta_synthesis_frontend.py'
      - 'scripts/meta_synthesis_frontend_fixture.mjs'
      - '.github/workflows/meta-synthesis-frontend-regression.yml'
  pull_request:
    branches: [main]
    paths:
      - 'monderman-report.js'
      - 'workspace-analysis.html'
      - 'workspace-diagnostics.html'
      - 'workspace.html'
      - 'workspace-actions.html'
      - 'workspace-assistant.js'
      - 'diagnostics.html'
      - 'cross-tool-synthesis.html'
      - 'scripts/validate_meta_synthesis_frontend.py'
      - 'scripts/meta_synthesis_frontend_fixture.mjs'
      - '.github/workflows/meta-synthesis-frontend-regression.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: meta-synthesis-frontend-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate depth and cross-lens customer contracts
        run: python scripts/validate_meta_synthesis_frontend.py

      - name: Run general frontend release validator
        run: python scripts/validate_frontend_release.py

      - name: Verify diff integrity
        run: git diff --check
