{
  "version": "report-overview-presentation-20260923.1",
  "renderer_sha256": "977cf5497c16b1e4427e5efa5113ff06e718e1331f18cde8c0069c0d10ba5791",
  "prior_renderer_sha256": "73c939fac58d0d2f7020207e6e1f869ec30e7f6ff745dbbfa6b76984b663ef98",
  "replacements": [
    [
      "    const scoreBand = m.kind === 'run' || m.selfRun ? firstStr(m.headlineBand) : firstStr(m.conditionBand);",
      "    const scoreBand = m.kind === 'run' ? firstStr(m.headlineBand) : firstStr(m.conditionBand);"
    ],
    [
      "  \"use strict\";\n  // This identifies the code displaying/exporting the report now, not the\n  // renderer that may have displayed a historical run when it was created.\n  const RENDERER_VERSION = \"diagnostic-renderer-report-overview-20260923.1\";\n  // The measured-report adapter stays at r43; financial reading order and\n  // summary presentation have their own explicit, independently tested edition.\n  const FINANCIAL_PRESENTATION_VERSION = \"financial-presentation-20260915.1\";\n",
      "  \"use strict\";\n  // This identifies the code displaying/exporting the report now, not the\n  // renderer that may have displayed a historical run when it was created.\n  const RENDERER_VERSION = \"diagnostic-renderer-evidence-reading-20260914.43\";\n  // The measured-report adapter stays at r43; financial reading order and\n  // summary presentation have their own explicit, independently tested edition.\n  const FINANCIAL_PRESENTATION_VERSION = \"financial-presentation-20260915.1\";\n"
    ],
    [
      "  }\n\n  function renderThreeBenefitFlow(s,level) {\n    if(!s?.coverage?.complete||!THREE_BENEFIT_CASES.includes(level))return '';\n    const input=s.inputs,panels=[],notes=[],groups=[];\n    const colors={currentRemaining:'#09383E',plannedRemaining:'#A9D0D4',remaining:'#09383E',spendingReduction:'#C9A227',spendingAvoidance:'#A9D0D4',staffCapacity:'#187783'};\n    const bound=v=>typeof v==='number'&&Number.isFinite(v)&&v>=0&&v<=1e14;\n    const difference=(a,b)=>{const v=a-b;return v<0&&Math.abs(v)<=8*Number.EPSILON*Math.max(1,a,b)?0:v;};\n    const attrs=row=>' data-baseline-value=\"'+row.baseline+'\" data-residual-value=\"'+row.residual+'\" data-released-value=\"'+row.released+'\"';\n    const unavailable=title=>notes.push(title+': the baseline cannot be drawn reliably. Refer to the saved inputs and planning table.');\n    if(!input||!Array.isArray(s.spendingItems)||!Array.isArray(s.activities))return '';\n    // Expense dollars use only entered current/planned expense baselines.\n    // Salary-valued retained hours and subscription costs never enter this flow.\n    let moneyValid=true;\n    const moneySources=[];\n    for(const [category,type,remainder]of [['spendingReduction','Current spending','currentRemaining'],['spendingAvoidance','Planned spending','plannedRemaining']]){\n      if(!Array.isArray(input[category]?.items)){moneyValid=false;continue;}\n      for(const item of input[category].items){\n        const result=s.spendingItems.find(row=>row.id===item.id);\n        if(!result){moneyValid=false;continue;}\n        const baseline=item.baselineMonthlyUnits*item.unitCost*(item.endMonth-item.startMonth+1);\n        moneySources.push({id:item.id,label:item.label,type,category,change:item.changeBasis,baseline,releasedByCase:result.amount,\n          flowsByCase:Object.fromEntries(THREE_BENEFIT_CASES.map(k=>[k,{currentRemaining:0,plannedRemaining:0,spendingReduction:0,spendingAvoidance:0,[category]:result.amount?.[k],[remainder]:difference(baseline,result.amount?.[k])}]))});\n      }\n    }\n    if(moneyValid)groups.push({kind:'money',title:'Money: current and planned spending',unit:'USD',sources:moneySources,\n      roles:['spendingReduction','spendingAvoidance','currentRemaining','plannedRemaining'],\n      colors:{...colors,spendingAvoidance:'#187783'},labels:{spendingReduction:'Potential current savings',spendingAvoidance:'Future spending avoided',currentRemaining:'Current spending that remains',plannedRemaining:'Planned spending still needed'},\n      shortLabels:{spendingReduction:'Current savings',spendingAvoidance:'Future avoided',currentRemaining:'Current remains',plannedRemaining:'Planned remains'}});\n    else unavailable('Money');\n    const cap=input.capacity,days=(Date.parse(cap?.measurementEnd)-Date.parse(cap?.measurementStart))/86400000;\n    if(Array.isArray(cap?.activities)&&(!cap.activities.length||(Number.isFinite(days)&&days>0))){\n      const timeSources=[];let timeValid=true;\n      for(const item of cap.activities){\n        const result=s.activities.find(row=>row.id===item.id);\n        if(!result){timeValid=false;continue;}\n        const baseline=item.measuredHours*(input.horizonMonths*365.25/12)/days;\n        timeSources.push({id:item.id,label:item.label,type:'Recorded staff time',category:'staffCapacity',change:item.changeBasis,baseline,releasedByCase:result.grossHoursFreed,\n          flowsByCase:Object.fromEntries(THREE_BENEFIT_CASES.map(k=>[k,{remaining:difference(baseline,result.grossHoursFreed?.[k]),spendingReduction:result.hoursUsedForSpendingReduction?.[k],spendingAvoidance:result.hoursUsedForSpendingAvoidance?.[k],staffCapacity:result.potentialHoursFreed?.[k]}]))});\n      }\n      if(timeValid)groups.push({kind:'workload',title:'Staff time: allocation and retained capacity',unit:'hours',sources:timeSources,\n        roles:['staffCapacity','spendingReduction','spendingAvoidance','remaining'],colors,\n        labels:{staffCapacity:'Time available for other work',spendingReduction:'Time reducing current spending',spendingAvoidance:'Time covering planned spending',remaining:'Work time that remains'},\n        shortLabels:{staffCapacity:'Other work',spendingReduction:'Current spending',spendingAvoidance:'Planned spending',remaining:'Time remaining'}});\n      else unavailable('Staff time');\n    }else unavailable('Staff time');\n    for(const group of groups){\n      if(!group.sources.length)continue; // No entered denominator is not zero burden.\n      const baseline=group.sources.reduce((n,row)=>n+row.baseline,0);\n      let valid=bound(baseline);\n      for(const row of group.sources){\n        if(!bound(row.baseline)||!THREE_BENEFIT_CASES.every(k=>bound(row.releasedByCase?.[k])&&group.roles.every(role=>bound(row.flowsByCase[k][role])))){valid=false;break;}\n        row.drawnByCase={};\n        for(const k of THREE_BENEFIT_CASES){\n          const flows=row.flowsByCase[k],released=row.releasedByCase[k];\n          row.drawnByCase[k]={...flows};\n          if(group.unit==='hours'){\n            const sum=flows.spendingReduction+flows.spendingAvoidance+flows.staffCapacity;\n            const tolerance=.02+8*Number.EPSILON*Math.max(1,sum,released);\n            if((sum===0)!==(released===0)||Math.abs(sum-released)>tolerance){valid=false;break;}\n            // Widths alone reconcile independently rounded centihours.\n            // Labels, detail and data attributes retain every saved amount.\n            for(const role of ['spendingReduction','spendingAvoidance','staffCapacity'])row.drawnByCase[k][role]=sum?flows[role]*released/sum:0;\n          }\n        }\n        row.released=row.releasedByCase[level];row.residual=difference(row.baseline,row.released);\n        row.flows=row.flowsByCase[level];row.drawnFlows=row.drawnByCase[level];\n      }\n      if(!valid||!Number.isFinite(180/baseline)){if(baseline!==0)unavailable(group.title);continue;}\n      if(baseline===0)continue;\n      const fmt=v=>group.unit==='USD'?threeBenefitHeadlineMoney(v):Number(v).toLocaleString('en-US',v>0&&v<1?{maximumSignificantDigits:3}:{maximumFractionDigits:0})+' h';\n      const exact=v=>group.unit==='USD'?threeBenefitMoney(v):threeBenefitNumber(v)+' hours';\n      const ordered=group.sources.slice().sort((a,b)=>b.baseline-a.baseline||String(a.id).localeCompare(String(b.id)));\n      const shown=ordered.length>6?ordered.slice(0,5):ordered.slice();\n      if(ordered.length>6){\n        const rest=ordered.slice(5),types=[...new Set(rest.map(row=>row.type))];\n        shown.push({id:'other',sourceIds:rest.map(row=>row.id),label:'Other recorded '+(group.unit==='hours'?'activities':'expenses')+' ('+rest.length+')',\n          type:types.length===1?types[0]:'Current and planned spending',baseline:rest.reduce((n,row)=>n+row.baseline,0),\n          flowsByCase:Object.fromEntries(THREE_BENEFIT_CASES.map(k=>[k,Object.fromEntries(group.roles.map(role=>[role,rest.reduce((n,row)=>n+row.flowsByCase[k][role],0)]))])),\n          drawnByCase:Object.fromEntries(THREE_BENEFIT_CASES.map(k=>[k,Object.fromEntries(group.roles.map(role=>[role,rest.reduce((n,row)=>n+row.drawnByCase[k][role],0)]))]))});\n      }\n      const scale=180/baseline,released=group.sources.reduce((n,row)=>n+row.released,0),residual=difference(baseline,released);\n      const sources=shown.map(row=>({...row,amount:row.baseline,drawnAmount:row.baseline,flows:row.flowsByCase[level],drawnFlows:row.drawnByCase[level]}));\n      const destinations=k=>group.roles.map(role=>({key:role,label:group.labels[role],shortLabel:group.shortLabels[role],type:'',amount:group.sources.reduce((n,row)=>n+row.flowsByCase[k][role],0),drawnAmount:group.sources.reduce((n,row)=>n+row.drawnByCase[k][role],0)}));\n      const dest=destinations(level);\n      // Bounded two-line titles keep the overview compact. Full source names\n      // and outcome definitions remain accessible and in the source detail.\n      // Amounts wrap in full, with a budget based on the largest baseline.\n      const labelHeight=(item,phone=false)=>(phone?32:36)+(item.type?(phone?17:18):0)+Math.ceil(fmt(baseline).length/(phone?7:12))*(phone?17:21)+5;\n      const allItems=[...sources,...dest];\n      const mobileRatio=Math.max(1,...allItems.map(item=>labelHeight(item,true)/labelHeight(item)));\n      const gapsFor=items=>items.slice(1).map((item,i)=>Math.max(16,(labelHeight(items[i])+labelHeight(item)-(items[i].drawnAmount+item.drawnAmount)*scale)/2+10));\n      const sourceGaps=gapsFor(sources),caseDestinations=THREE_BENEFIT_CASES.map(destinations);\n      const gapBudget=Math.max(sourceGaps.reduce((a,b)=>a+b,0),...caseDestinations.map(items=>gapsFor(items).reduce((a,b)=>a+b,0)));\n      const padding=Math.max(...allItems.map(item=>labelHeight(item)))/2+8,extent=180+gapBudget,height=extent+2*padding;\n      const position=items=>{\n        const gaps=gapsFor(items),extra=items.length>1?(gapBudget-gaps.reduce((a,b)=>a+b,0))/(items.length-1):0;\n        let y=padding;\n        for(let i=0;i<items.length;i++){const item=items[i];item.top=y;item.center=y+item.drawnAmount*scale/2;item.offset=y;y+=item.drawnAmount*scale+(gaps[i]||0)+(i<items.length-1?extra:0);}\n      };\n      position(sources);position(dest);\n      if(sources.length===1)sources[0].center=height/2;\n      const compactTypes={'Current spending':'Current','Planned spending':'Planned','Recorded staff time':'Staff time','Current and planned spending':'Mixed'};\n      const label=(item,right)=>{\n        const short=right?item.shortLabel:item.label.length>28?item.label.slice(0,27).trimEnd()+'…':item.label;\n        return '<div class=\"mr-unified-label\" data-label-kind=\"'+(right?'outcome':'source')+'\" style=\"top:'+(item.center/height*100)+'%\"><span class=\"mr-unified-title\" title=\"'+esc(item.label)+'\"><span class=\"mr-unified-short-title\" aria-hidden=\"true\">'+esc(short)+'</span><span class=\"mr-unified-full-title\">'+esc(item.label)+'</span></span>'+(item.type?'<small title=\"'+esc(item.type)+'\">'+esc(compactTypes[item.type]||item.type)+'</small>':'')+'<strong data-node-value=\"'+item.amount+'\">'+esc(fmt(item.amount))+'</strong></div>';\n      };\n      let paths='',nodes='';\n      const rect=(x,y,amount,color,side,id)=>'<rect x=\"'+x+'\" y=\"'+y+'\" width=\"6\" height=\"'+(amount*scale)+'\" fill=\"'+color+'\" data-node-side=\"'+side+'\" data-node-id=\"'+esc(id)+'\" data-node-amount=\"'+amount+'\"/>';\n      for(const source of sources){\n        let start=source.top;\n        for(const target of dest){\n          const amount=source.flows[target.key],drawnAmount=source.drawnFlows[target.key],width=drawnAmount*scale,end=target.offset;\n          if(sources.length===1)start=end;\n          if(amount>0){\n            paths+='<path d=\"M6 '+start+' C70 '+start+',130 '+end+',194 '+end+' L194 '+(end+width)+' C130 '+(end+width)+',70 '+(start+width)+',6 '+(start+width)+' Z\" fill=\"'+group.colors[target.key]+'\" fill-opacity=\".34\" data-burden-role=\"'+target.key+'\" data-burden-amount=\"'+amount+'\" data-burden-drawn-amount=\"'+drawnAmount+'\" data-burden-source-ids=\"'+esc((source.sourceIds||[source.id]).join(' '))+'\"><title>'+esc(source.label+' → '+target.label+': '+exact(amount))+'</title></path>';\n          }\n          if(sources.length===1)nodes+=rect(0,start,drawnAmount,'#09383E','source',source.id);\n          start+=width;target.offset+=width;\n        }\n        if(sources.length>1)nodes+=rect(0,source.top,source.baseline,'#09383E','source',source.id);\n      }\n      nodes+=dest.map(item=>rect(194,item.top,item.drawnAmount,group.colors[item.key],'outcome',item.key)).join('');\n      const detail=group.sources.map(row=>'<li data-burden-source=\"'+esc(row.id)+'\" data-burden-category=\"'+row.category+'\"'+attrs(row)+(group.unit==='hours'?' data-spending-reduction-hours=\"'+row.flows.spendingReduction+'\" data-spending-avoidance-hours=\"'+row.flows.spendingAvoidance+'\" data-retained-hours=\"'+row.flows.staffCapacity+'\"':'')+'><strong>'+esc(row.label)+'</strong><span>'+esc(row.type+' · baseline '+exact(row.baseline)+' · remains '+exact(row.residual)+' · potential '+(group.unit==='hours'?'time released ':'spending benefit ')+exact(row.released))+'</span><span>'+esc(row.change)+'</span></li>').join('');\n      const summary=group.unit==='hours'?'recorded-work baseline':'current and planned expense baseline';\n      panels.push('<section class=\"mr-burden-section\" data-burden-kind=\"'+group.kind+'\" data-burden-unit=\"'+group.unit+'\" data-burden-case=\"'+level+'\" data-burden-scale=\"'+scale+'\"'+attrs({baseline,residual,released})+'><h5>'+group.title+'</h5><p class=\"mr-burden-summary\"><strong>'+esc(fmt(baseline))+'</strong> '+summary+' → <strong>'+esc(fmt(residual))+'</strong> remains; <strong>'+esc(fmt(released))+'</strong> '+(group.unit==='hours'?'could be released':'in potential spending benefits')+'.</p><div class=\"mr-unified-heading\"><span>Baseline · '+input.horizonMonths+' months</span><span>After the proposed changes</span></div><div class=\"mr-unified-flow\" data-flow-direction=\"left-to-right\" style=\"--burden-height:'+height+'px;--burden-mobile-height:'+(height*mobileRatio)+'px\"><div class=\"mr-unified-labels\">'+sources.map(item=>label(item,false)).join('')+'</div><svg viewBox=\"0 0 200 '+height+'\" preserveAspectRatio=\"none\" role=\"img\" aria-label=\"'+esc(group.title+': '+exact(baseline)+' baseline, '+exact(residual)+' remains')+'\"><desc>'+esc(dest.map(item=>item.label+' '+exact(item.amount)).join('; '))+'.</desc>'+paths+nodes+'</svg><div class=\"mr-unified-labels\">'+dest.map(item=>label(item,true)).join('')+'</div></div>'+(ordered.length>6?'<p class=\"mr-benefit-chart-note\">Other combines '+(ordered.length-5)+' recorded '+(group.unit==='hours'?'activities':'expenses')+'. Every source and its full amount is listed below.</p>':'')+'<details class=\"mr-burden-source-details\"><summary>All '+group.sources.length+' sources and proposed changes</summary><ul>'+detail+'</ul></details><p class=\"mr-benefit-chart-note\">'+(group.unit==='hours'?'Released time assigned to current or planned spending is excluded from time available for other work. Hours and dollars describe related effects; do not add them together.':'Current savings and avoided future spending remain separate outcomes. Avoided spending is measured against documented plans, not a bank-balance increase. Salary-valued retained capacity is not included in this money chart.')+'</p></section>');\n    }\n    if(!panels.length)return notes.length?'<p class=\"mr-benefit-chart-note\">'+notes.map(esc).join(' ')+'</p>':'';\n    const style='<style>.mr-benefit-chart[data-flow-version=\"20260923.1\"]{margin:24px 0;min-width:0}.mr-burden-section{padding:20px 0;border-top:1px solid #DCE5E8;min-width:0}.mr-burden-section h5{font-size:1.05rem;margin:0 0 8px}.mr-burden-summary{font-size:.9rem;line-height:1.5}.mr-burden-summary strong{color:#09383E}.mr-unified-heading{display:flex;justify-content:space-between;gap:18px;font-size:12px;font-weight:600;color:#53676E;margin-top:18px}.mr-unified-heading>span{width:32%;min-width:0}.mr-unified-heading>span:last-child{text-align:right}.mr-unified-flow{display:grid;grid-template-rows:minmax(0,1fr);grid-template-columns:minmax(0,1fr) minmax(52px,1.4fr) minmax(0,1fr);column-gap:12px;height:var(--burden-height);min-width:0;margin:8px 0 12px}.mr-unified-flow>svg{display:block;width:100%;height:100%;min-width:0;overflow:visible}.mr-unified-labels{position:relative;min-width:0;height:100%}.mr-unified-label{position:absolute;left:0;right:0;transform:translateY(-50%);font-size:13px;line-height:18px;overflow-wrap:anywhere;word-break:normal}.mr-unified-label>span,.mr-unified-label>strong{display:block}.mr-unified-short-title{display:-webkit-box;-webkit-line-clamp:2;line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;text-overflow:ellipsis}.mr-unified-full-title{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}.mr-unified-label>small{display:block}.mr-unified-label>small{font-size:11px;line-height:15px;color:#53676E;margin-top:3px}.mr-unified-label>strong{font-size:17px;line-height:21px;color:#09383E;margin-top:5px;white-space:normal;font-variant-numeric:tabular-nums}.mr-burden-source-details{font-size:.8rem;overflow-wrap:anywhere}.mr-burden-source-details summary{cursor:pointer;color:#187783;padding:10px 0;min-height:44px}.mr-burden-source-details li{margin:10px 0}.mr-burden-source-details li>span{display:block;margin-top:4px}.mr-burden-source-details summary:focus-visible{outline:3px solid #187783;outline-offset:3px}@media screen and (max-width:640px){.mr-unified-flow{height:var(--burden-mobile-height);column-gap:6px;grid-template-columns:minmax(0,1fr) minmax(40px,1.2fr) minmax(0,1fr)}.mr-unified-label{font-size:12px;line-height:16px}.mr-unified-label>small{font-size:11px;line-height:14px}.mr-unified-label>strong{font-size:13px;line-height:17px}.mr-unified-heading{font-size:11px}.mr-burden-summary{font-size:13px}}@media print{.mr-benefit-chart[data-flow-version]{break-inside:auto}.mr-burden-section{break-inside:avoid;padding:10px 0}.mr-burden-section h5{font-size:11px}.mr-report .mr-burden-summary{font-size:9px;margin:6px 0}.mr-unified-flow{height:calc(var(--burden-height)*.60);column-gap:8px}.mr-unified-label{font-size:8px;line-height:10px}.mr-unified-label>small{font-size:7px;line-height:9px}.mr-unified-label>strong{font-size:10px;line-height:12px;margin-top:3px}.mr-unified-heading{font-size:8px;margin-top:6px}.mr-burden-source-details{display:none}}</style>';\n    return '<figure class=\"mr-benefit-chart\" data-flow-version=\"20260923.1\">'+style+'<h4>'+humanize(level)+' case: money and staff time</h4><p class=\"mr-benefit-chart-note\">Two views of the proposed change, before costs. The baseline covers entered activities and expenses, not all organizational work or spending. Costs and net value are shown separately.</p>'+panels.join('')+notes.map(note=>'<p class=\"mr-benefit-chart-note\">'+esc(note)+'</p>').join('')+'<figcaption class=\"mr-benefit-chart-note\">Directional estimates based on collected self-reported sample data, recorded operational inputs and stated assumptions. Each diagram uses one unit and a fixed scale across Low, Central and High. Translucent ribbons reconcile minor rounding differences; the tables retain the saved values. Staff time is not automatically cash savings. Full item-by-item sources and assumptions follow in the report.</figcaption></figure>';\n  }\n\n  function renderThreeBenefitBrief(m) {\n",
      "  }\n\n  function renderThreeBenefitFlow(s,level) {\n    if(!s.coverage.complete)return '';\n    const input=s.inputs,colors={remaining:'#C4D0D2',spendingReduction:'#0C6E78',spendingAvoidance:'#C9A227',staffCapacity:'#4E8991'},groups=[];\n    // Only the baseline is reconstructed. Benefits and hour allocations come\n    // unchanged from the reconciled saved scenario, never from scores.\n    for(const [key,kind,title] of [['spendingReduction','current-spending','Current spending'],['spendingAvoidance','planned-spending','Planned spending']]){\n      const sources=input[key].items.map(a=>{const r=s.spendingItems.find(x=>x.id===a.id);return {id:a.id,label:a.label,change:a.changeBasis,baseline:a.baselineMonthlyUnits*a.unitCost*(a.endMonth-a.startMonth+1),released:r.amount[level],flows:{[key]:r.amount[level]}};});\n      groups.push({kind,title,unit:'USD',sources,roles:['remaining',key],labels:{remaining:key==='spendingReduction'?'Spending that remains':'Planned spending still needed',[key]:key==='spendingReduction'?'Potential current savings':'Potential future spending avoided'}});\n    }\n    const cap=input.capacity,days=(Date.parse(cap.measurementEnd)-Date.parse(cap.measurementStart))/86400000;\n    groups.push({kind:'workload',title:'Staff time',unit:'hours',roles:['remaining','spendingReduction','spendingAvoidance','staffCapacity'],labels:{remaining:'Work time that remains',spendingReduction:'Time reducing current spending',spendingAvoidance:'Time covering planned spending',staffCapacity:'Time available for other work'},sources:cap.activities.map(a=>{const r=s.activities.find(x=>x.id===a.id);return {id:a.id,label:a.label,change:a.changeBasis,baseline:a.measuredHours*(input.horizonMonths*365.25/12)/days,released:r.grossHoursFreed[level],flows:{spendingReduction:r.hoursUsedForSpendingReduction[level],spendingAvoidance:r.hoursUsedForSpendingAvoidance[level],staffCapacity:r.potentialHoursFreed[level]}};})});\n    const bound=v=>Number.isFinite(v)&&v>=0&&v<=1e14,number=v=>Number(v).toLocaleString('en-US',v>0&&v<1?{maximumSignificantDigits:3}:{maximumFractionDigits:0}),notes=[],panels=[];\n    const difference=(a,b)=>{const v=a-b;return v<0&&Math.abs(v)<=8*Number.EPSILON*Math.max(1,a,b)?0:v;};\n    const attrs=row=>' data-baseline-value=\"'+row.baseline+'\" data-residual-value=\"'+row.residual+'\" data-released-value=\"'+row.released+'\"';\n    const keyboard=\"if(event.target===this&&['ArrowLeft','ArrowRight','Home','End'].includes(event.key)){event.preventDefault();this.scrollTo({left:event.key==='Home'?0:event.key==='End'?this.scrollWidth:this.scrollLeft+(event.key==='ArrowLeft'?-80:80),behavior:'instant'});}\";\n    for(const group of groups){\n      if(!group.sources.length)continue; // No baseline supplied is not zero burden.\n      for(const row of group.sources){row.residual=difference(row.baseline,row.released);row.flows.remaining=row.residual;}\n      const baseline=group.sources.reduce((n,r)=>n+r.baseline,0),released=group.sources.reduce((n,r)=>n+r.released,0),residual=difference(baseline,released);\n      if(!bound(baseline)||!bound(residual)||group.sources.some(r=>!bound(r.baseline)||!bound(r.residual))){notes.push(group.title+': the baseline cannot be drawn reliably. Refer to the saved inputs and planning table.');continue;}\n      if(baseline===0)continue;\n      if(!Number.isFinite(260/baseline)){notes.push(group.title+': the baseline cannot be drawn reliably. Refer to the saved inputs and planning table.');continue;}\n      // Independently rounded hour components may differ by a centihour from\n      // their saved gross total. Reconcile WIDTHS only, proportionally; labels\n      // and data-burden-amount keep the exact saved component values.\n      let drawable=true;\n      for(const row of group.sources){\n        row.drawnFlows={...row.flows};\n        if(group.unit==='hours'){\n          const sum=row.flows.spendingReduction+row.flows.spendingAvoidance+row.flows.staffCapacity;\n          if((sum===0)!==(row.released===0)){drawable=false;break;}\n          for(const key of ['spendingReduction','spendingAvoidance','staffCapacity'])row.drawnFlows[key]=sum?row.flows[key]*row.released/sum:0;\n        }\n      }\n      if(!drawable){notes.push(group.title+': rounded components are too small to draw reliably. Refer to the saved inputs and planning table.');continue;}\n      const fmt=v=>group.unit==='USD'?threeBenefitHeadlineMoney(v):number(v)+' h',exact=v=>group.unit==='USD'?threeBenefitMoney(v):threeBenefitNumber(v)+' hours';\n      const ordered=group.sources.slice().sort((a,b)=>b.baseline-a.baseline||String(a.id).localeCompare(String(b.id))),shown=ordered.length>4?ordered.slice(0,3):ordered.slice();\n      if(ordered.length>4){const rest=ordered.slice(3);shown.push({label:'Other recorded '+(group.unit==='hours'?'activities':'expenses')+' ('+rest.length+')',baseline:rest.reduce((n,r)=>n+r.baseline,0),flows:Object.fromEntries(group.roles.map(key=>[key,rest.reduce((n,r)=>n+r.flows[key],0)])),drawnFlows:Object.fromEntries(group.roles.map(key=>[key,rest.reduce((n,r)=>n+r.drawnFlows[key],0)]))});}\n      // One denominator and scale for all three cases. Neutral ribbons keep\n      // the unchanged workload/spend visible; they are not called waste.\n      const scale=220/baseline,dest=group.roles.map(key=>({key,label:group.labels[key],amount:group.sources.reduce((n,r)=>n+r.flows[key],0),drawnAmount:group.sources.reduce((n,r)=>n+r.drawnFlows[key],0)}));\n      const extent=items=>items.reduce((n,item)=>n+Math.max(82,item.drawnAmount*scale),0)+24*(items.length-1);\n      const sources=shown.map(row=>({...row,amount:row.baseline,drawnAmount:row.baseline})),height=Math.max(extent(sources),extent(dest))+24;\n      const position=items=>{let y=(height-extent(items))/2;for(const item of items){const h=Math.max(82,item.drawnAmount*scale);item.center=y+h/2;item.offset=item.center-item.drawnAmount*scale/2;y+=h+24;}};\n      position(sources);position(dest);\n      const label=(item,right)=>'<div class=\"mr-benefit-flow-node'+(right?' is-right':'')+'\" style=\"--benefit-node-center:'+(item.center/height*100)+'%;--benefit-color:'+colors[right?item.key:'remaining']+'\"><span>'+esc(item.label)+'</span><strong>'+esc(fmt(item.amount))+'</strong></div>';\n      let paths='',nodes='';\n      for(const source of sources){let start=source.offset;for(const target of dest){const amount=source.flows[target.key],drawnAmount=source.drawnFlows[target.key],width=drawnAmount*scale;if(amount>0){const end=target.offset;paths+='<path d=\"M228 '+start+' C344 '+start+',456 '+end+',572 '+end+' L572 '+(end+width)+' C456 '+(end+width)+',344 '+(start+width)+',228 '+(start+width)+' Z\" fill=\"'+colors[target.key]+'\" data-burden-role=\"'+target.key+'\" data-burden-amount=\"'+amount+'\" data-burden-drawn-amount=\"'+drawnAmount+'\"><title>'+esc(source.label+' → '+target.label+': '+exact(amount))+'</title></path>';start+=width;target.offset+=width;}}nodes+=label(source,false);}\n      nodes+=dest.map(item=>label(item,true)).join('');\n      // Phone overview retains the full baseline and outcome split on screen.\n      // Individual sources remain named above it and in the expandable detail.\n      const mobileScale=260/baseline;\n      let mobilePaths='',mobileStart=30,mobileEnd=10;\n      for(const target of dest){\n        if(target.key==='remaining')mobileEnd=10;\n        else if(target===dest[1])mobileEnd=30+residual*mobileScale;\n        const w=target.drawnAmount*mobileScale;\n        if(w>0)mobilePaths+='<path d=\"M'+mobileStart+' 12 C'+mobileStart+' 84,'+mobileEnd+' 104,'+mobileEnd+' 178 L'+(mobileEnd+w)+' 178 C'+(mobileEnd+w)+' 104,'+(mobileStart+w)+' 84,'+(mobileStart+w)+' 12 Z\" fill=\"'+colors[target.key]+'\" data-mobile-burden-role=\"'+target.key+'\" data-mobile-burden-amount=\"'+target.drawnAmount+'\"><title>'+esc(target.label+': '+exact(target.amount))+'</title></path>';\n        mobileStart+=w;mobileEnd+=w;\n      }\n      const mobile='<div class=\"mr-burden-mobile\" data-mobile-burden-scale=\"'+mobileScale+'\"><ul class=\"mr-burden-mobile-sources\">'+shown.map(row=>'<li><span>'+esc(row.label)+'</span><strong>'+esc(fmt(row.baseline))+'</strong></li>').join('')+'</ul><p class=\"mr-burden-mobile-total\">'+esc(fmt(baseline))+' baseline</p><svg viewBox=\"0 0 320 190\" role=\"img\" aria-label=\"'+esc(group.title+': '+exact(baseline)+' baseline; '+exact(residual)+' remains; '+exact(released)+' could be '+(group.unit==='hours'?'released':'saved'))+'\">'+mobilePaths+'</svg><div class=\"mr-burden-mobile-outcomes\"><div><span>Remains</span><strong>'+esc(fmt(residual))+'</strong></div><div><span>'+(group.unit==='hours'?'Time released':'Potential savings')+'</span><strong>'+esc(fmt(released))+'</strong></div></div>'+(group.unit==='hours'?'<ul class=\"mr-burden-mobile-allocation\">'+dest.filter(x=>x.key!=='remaining').map(x=>'<li style=\"--benefit-color:'+colors[x.key]+'\"><span>'+esc(x.label)+'</span><strong>'+esc(fmt(x.amount))+'</strong></li>').join('')+'</ul>':'')+'</div>';\n      const detail=group.sources.map(row=>'<li data-burden-source=\"'+esc(row.id)+'\"'+attrs(row)+(group.unit==='hours'?' data-spending-reduction-hours=\"'+row.flows.spendingReduction+'\" data-spending-avoidance-hours=\"'+row.flows.spendingAvoidance+'\" data-retained-hours=\"'+row.flows.staffCapacity+'\"':'')+'><strong>'+esc(row.label)+'</strong><span>'+esc('Baseline '+exact(row.baseline)+' · remains '+exact(row.residual)+' · potential '+(group.unit==='hours'?'time released ':'saving ')+exact(row.released))+'</span><span>'+esc(row.change)+'</span></li>').join('');\n      panels.push('<section class=\"mr-burden-section\" data-burden-kind=\"'+group.kind+'\" data-burden-unit=\"'+group.unit+'\" data-burden-case=\"'+level+'\" data-burden-scale=\"'+scale+'\"'+attrs({baseline,residual,released})+'><h5>'+group.title+'</h5><p class=\"mr-burden-summary\"><strong>'+esc(fmt(baseline))+'</strong> '+(group.unit==='hours'?'recorded-work baseline':'entered expense baseline')+' → <strong>'+esc(fmt(residual))+'</strong> remains; <strong>'+esc(fmt(released))+'</strong> '+(group.unit==='hours'?'could be released':'could be saved')+'.</p><div class=\"mr-benefit-flow-heading\"><span>Baseline · '+input.horizonMonths+' months</span><span>After the proposed changes</span></div><div class=\"mr-benefit-scroll\" role=\"region\" tabindex=\"0\" onkeydown=\"'+esc(keyboard)+'\" aria-label=\"'+esc(humanize(level)+' '+group.title+' Sankey')+'\"><div class=\"mr-benefit-flow\" style=\"--burden-height:'+height+'px\"><svg viewBox=\"0 0 800 '+height+'\" preserveAspectRatio=\"none\" role=\"img\" aria-label=\"'+esc(group.title+': baseline to remaining and potential savings')+'\"><desc>'+esc('Baseline '+exact(baseline)+'. '+dest.map(item=>item.label+' '+exact(item.amount)).join('; '))+'.</desc>'+paths+'</svg><div class=\"mr-benefit-flow-labels\">'+nodes+'</div></div></div>'+mobile+'<details class=\"mr-burden-source-details\"><summary>Sources and proposed changes</summary><ul>'+detail+'</ul></details>'+(group.kind==='workload'?'<p class=\"mr-benefit-chart-note\">Released time assigned to current or planned spending is excluded from time available for other work. The hours and dollars describe related effects; do not add them together.</p>':'')+'</section>');\n    }\n    if(!panels.length)return notes.length?'<p class=\"mr-benefit-chart-note\">'+notes.map(esc).join(' ')+'</p>':'';\n    const style='<style>.mr-benefit-chart[data-flow-version=\"20260921.1\"]{margin:28px 0}.mr-burden-section{padding:22px 0;border-top:1px solid #DCE5E8}.mr-burden-section h5{font-size:1.05rem;margin:0 0 8px}.mr-burden-summary{font-size:.9rem;line-height:1.5}.mr-burden-summary strong{color:#08383E}.mr-benefit-chart[data-flow-version] .mr-benefit-scroll{overflow-x:auto;overflow-y:hidden;margin:12px 0 16px}.mr-benefit-chart[data-flow-version] .mr-benefit-flow{min-width:800px;height:var(--burden-height)!important}.mr-benefit-chart[data-flow-version] .mr-benefit-flow-node{top:var(--benefit-node-center)!important;transform:translateY(-50%);width:27%;min-height:0;padding:6px 10px;font-size:13px;line-height:1.3;border-color:var(--benefit-color)}.mr-benefit-chart[data-flow-version] .mr-benefit-flow-node span{display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.mr-benefit-chart[data-flow-version] .mr-benefit-flow-node strong{font-size:17px}.mr-benefit-flow-heading{display:flex;justify-content:space-between;gap:20px;font-size:.8rem;font-weight:700;color:#53676E;margin-top:18px}.mr-benefit-flow-heading span:last-child{text-align:right}.mr-benefit-flow-hint{font-size:12px;color:#53676E}.mr-burden-source-details{font-size:.8rem}.mr-burden-source-details summary{cursor:pointer;color:#0C6E78;padding:10px 0;min-height:44px}.mr-burden-source-details li{margin:10px 0}.mr-burden-source-details li>span{display:block;margin-top:4px}.mr-burden-mobile{display:none}@media screen and (max-width:640px){.mr-burden-section>.mr-benefit-scroll,.mr-burden-section>.mr-benefit-flow-heading,.mr-benefit-flow-hint{display:none}.mr-burden-mobile{display:block}.mr-burden-mobile-sources{list-style:none;padding:0;margin:16px 0;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.mr-burden-mobile-sources li{border-left:3px solid #C4D0D2;padding-left:9px;font-size:12px;line-height:1.4;min-width:0}.mr-burden-mobile-sources span,.mr-burden-mobile-sources strong{display:block}.mr-burden-mobile-total{text-align:center;font-weight:700;font-size:14px}.mr-burden-mobile svg{display:block;width:100%;height:auto}.mr-burden-mobile-outcomes{display:grid;grid-template-columns:1fr 1fr;gap:12px;font-size:13px;margin-bottom:14px}.mr-burden-mobile-outcomes>div:last-child{text-align:right}.mr-burden-mobile-outcomes strong{display:block;font-size:18px;margin-top:4px}.mr-burden-mobile-allocation{list-style:none;padding:0;margin:16px 0}.mr-burden-mobile-allocation li{display:flex;justify-content:space-between;gap:12px;border-left:3px solid var(--benefit-color);padding:8px 0 8px 10px;font-size:12px;line-height:1.4}.mr-burden-mobile-allocation strong{white-space:nowrap}.mr-burden-summary{font-size:13px}}@media screen and (min-width:1000px){.mr-benefit-flow-hint{display:none}}@media print{.mr-benefit-chart[data-flow-version]{break-inside:auto}.mr-burden-section{break-inside:avoid;padding:12px 0}.mr-burden-section h5{font-size:11px}.mr-report .mr-burden-summary{font-size:9px;margin:6px 0}.mr-benefit-chart[data-flow-version] .mr-benefit-flow{min-width:0;height:calc(var(--burden-height)*.60)!important}.mr-benefit-chart[data-flow-version] .mr-benefit-scroll{overflow:visible;margin:8px 0}.mr-benefit-chart[data-flow-version] .mr-benefit-flow-node{font-size:8px;line-height:1.2;padding:3px 5px}.mr-benefit-chart[data-flow-version] .mr-benefit-flow-node strong{font-size:10px}.mr-benefit-flow-heading{font-size:8px;margin-top:6px}.mr-benefit-flow-hint,.mr-burden-source-details{display:none}}</style>';\n    return '<figure class=\"mr-benefit-chart\" data-flow-version=\"20260921.1\">'+style+'<h4>'+humanize(level)+' case: from current demands to potential savings</h4><p class=\"mr-benefit-chart-note\">See where recorded time and spending could change, and what remains. These are potential benefits before costs. Costs and net value are shown separately. The baseline covers the entered activities and expenses, not all organizational work or spending.</p><p class=\"mr-benefit-flow-hint\">Swipe across each diagram, or focus it and use the arrow keys. The summary above each diagram gives the same totals.</p>'+panels.join('')+notes.map(note=>'<p class=\"mr-benefit-chart-note\">'+esc(note)+'</p>').join('')+'<figcaption class=\"mr-benefit-chart-note\">Directional estimates based on collected self-reported sample data, recorded operational inputs and stated assumptions. Each diagram uses one unit and a fixed scale across Low, Central and High. Ribbon widths reconcile minor rounding differences; the tables retain the saved values. Staff time is not automatically cash savings.</figcaption></figure>';\n  }\n\n  function renderThreeBenefitBrief(m) {\n"
    ],
    [
      "    const valid=threeBenefitPresentation(m);if(!valid)return '';\n    const {s,input:i}=valid,percent=v=>threeBenefitNumber(v)+'%',facts=rows=>'<dl>'+rows.map(([k,v])=>'<dt>'+esc(k)+'</dt><dd>'+esc(v)+'</dd>').join('')+'</dl>';\n    let html='<section class=\"mr-section mr-three-benefit mr-benefit-assumptions\"><h2>'+n+'. Operational inputs and reconciliation</h2><p>Each value comes from the saved scenario, not from a diagnostic score. Spending reductions use an existing expense baseline; avoided future spending uses a documented planned expense. Retained staff capacity excludes every hour assigned to either spending category.</p><p>'+esc(obj(s.method).extrapolation)+'</p>';\n    if(obj(s.method).capacityCostSource==='matched_campaign_population')html+='<p class=\"mr-employment-cost-basis\">Staff capacity is valued using the average hourly employment cost of the entire named campaign population, calculated from uploaded annual base salaries and the saved annual working hours and benefits/overhead settings. Each person is counted once across lenses. This assumes the measured activities reflect that whole group’s typical mix of work. It values staff capacity, not cash savings.</p>';\n    for(const key of THREE_BENEFIT_KEYS)html+='<h3>'+THREE_BENEFIT_LABELS[key]+'</h3><p>'+esc(s.benefits[key].basis)+' · '+esc(humanize(s.benefits[key].status))+'</p>';\n    for(const a of i.capacity.activities){const r=s.activities.find(x=>x.id===a.id);html+='<article class=\"mr-benefit-assumption\"><h3>'+esc(a.label)+'</h3>'+facts([['Measurement window',i.capacity.measurementStart+' to '+i.capacity.measurementEnd],['People covered by records',i.capacity.measuredPeople],['Measured activity hours',threeBenefitNumber(a.measuredHours)],['Loaded hourly cost',threeBenefitMoney(a.loadedHourlyCost)],['Source basis',humanize(a.sourceBasis)],['Source reference',a.sourceReference],['Proposed change',a.changeBasis]])+threeBenefitTable('Activity assumptions',[['Reduction assumption',a.reductionPercent,percent],['Adoption assumption',a.adoptionPercent,percent]])+threeBenefitTable('Saved activity reconciliation',[['Gross hours freed',r.grossHoursFreed,threeBenefitNumber],['Hours assigned to current spending',r.hoursUsedForSpendingReduction,threeBenefitNumber],['Hours assigned to future spending',r.hoursUsedForSpendingAvoidance,threeBenefitNumber],['Retained staff hours',r.potentialHoursFreed,threeBenefitNumber],['Retained capacity value',r.capacityValue]])+'</article>';}\n    for(const key of ['spendingReduction','spendingAvoidance'])for(const item of i[key].items){const r=s.spendingItems.find(x=>x.id===item.id);html+='<article class=\"mr-benefit-assumption\"><h3>'+esc(item.label)+'</h3>'+facts([['Benefit category',THREE_BENEFIT_LABELS[key]],['Resource reference',item.resourceId],['Expense type',humanize(item.kind)],['Baseline each month',threeBenefitNumber(item.baselineMonthlyUnits)+' '+item.unit],['Cost per unit',threeBenefitMoney(item.unitCost)],['Active planning months',item.startMonth+' to '+item.endMonth+' ('+r.activeMonths+' months)'],['Linked capacity activity',item.capacityActivityId||'Not applicable: non-labor expense'],['Source reference',item.sourceReference],['Proposed change',item.changeBasis]])+threeBenefitTable('Spending assumptions',[['Reduction assumption',item.reductionPercent,percent],['Adoption assumption',item.adoptionPercent,percent]])+threeBenefitTable('Saved spending benefit',[['Spending benefit',r.amount],['Capacity hours assigned to this expense',r.allocatedHours,threeBenefitNumber]])+'</article>';}\n",
      "    const valid=threeBenefitPresentation(m);if(!valid)return '';\n    const {s,input:i}=valid,percent=v=>threeBenefitNumber(v)+'%',facts=rows=>'<dl>'+rows.map(([k,v])=>'<dt>'+esc(k)+'</dt><dd>'+esc(v)+'</dd>').join('')+'</dl>';\n    let html='<section class=\"mr-section mr-three-benefit mr-benefit-assumptions\"><h2>'+n+'. Operational inputs and reconciliation</h2><p>Each value comes from the saved scenario, not from a diagnostic score. Spending reductions use an existing expense baseline; avoided future spending uses a documented planned expense. Retained staff capacity excludes every hour assigned to either spending category.</p><p>'+esc(obj(s.method).extrapolation)+'</p>';\n    for(const key of THREE_BENEFIT_KEYS)html+='<h3>'+THREE_BENEFIT_LABELS[key]+'</h3><p>'+esc(s.benefits[key].basis)+' · '+esc(humanize(s.benefits[key].status))+'</p>';\n    for(const a of i.capacity.activities){const r=s.activities.find(x=>x.id===a.id);html+='<article class=\"mr-benefit-assumption\"><h3>'+esc(a.label)+'</h3>'+facts([['Measurement window',i.capacity.measurementStart+' to '+i.capacity.measurementEnd],['People covered by records',i.capacity.measuredPeople],['Measured activity hours',threeBenefitNumber(a.measuredHours)],['Loaded hourly cost',threeBenefitMoney(a.loadedHourlyCost)],['Source basis',humanize(a.sourceBasis)],['Source reference',a.sourceReference],['Proposed change',a.changeBasis]])+threeBenefitTable('Activity assumptions',[['Reduction assumption',a.reductionPercent,percent],['Adoption assumption',a.adoptionPercent,percent]])+threeBenefitTable('Saved activity reconciliation',[['Gross hours freed',r.grossHoursFreed,threeBenefitNumber],['Hours assigned to current spending',r.hoursUsedForSpendingReduction,threeBenefitNumber],['Hours assigned to future spending',r.hoursUsedForSpendingAvoidance,threeBenefitNumber],['Retained staff hours',r.potentialHoursFreed,threeBenefitNumber],['Retained capacity value',r.capacityValue]])+'</article>';}\n    for(const key of ['spendingReduction','spendingAvoidance'])for(const item of i[key].items){const r=s.spendingItems.find(x=>x.id===item.id);html+='<article class=\"mr-benefit-assumption\"><h3>'+esc(item.label)+'</h3>'+facts([['Benefit category',THREE_BENEFIT_LABELS[key]],['Resource reference',item.resourceId],['Expense type',humanize(item.kind)],['Baseline each month',threeBenefitNumber(item.baselineMonthlyUnits)+' '+item.unit],['Cost per unit',threeBenefitMoney(item.unitCost)],['Active planning months',item.startMonth+' to '+item.endMonth+' ('+r.activeMonths+' months)'],['Linked capacity activity',item.capacityActivityId||'Not applicable: non-labor expense'],['Source reference',item.sourceReference],['Proposed change',item.changeBasis]])+threeBenefitTable('Spending assumptions',[['Reduction assumption',item.reductionPercent,percent],['Adoption assumption',item.adoptionPercent,percent]])+threeBenefitTable('Saved spending benefit',[['Spending benefit',r.amount],['Capacity hours assigned to this expense',r.allocatedHours,threeBenefitNumber]])+'</article>';}\n"
    ],
    [
      "\n  const screenReportModels = new WeakMap();\n\n  // An orientation layer over the saved report, never a second calculation or\n  // a replacement for its evidence, assumptions and complete recommendations.\n  function buildReportOverview(model, sections) {\n    const m = obj(model), find = pattern => sections.find(s => pattern.test(s.classes));\n    const ai = obj(m.aiReport), interpretation = ai.status === 'complete' ? obj(obj(ai.report).interpretation) : {};\n    const findings = find(/mr-run-headline|mr-system-read|mr-depth-system-read|mr-ai-interpretation/) || sections[1];\n    const financial = find(/mr-financial-brief/), profile = find(/mr-run-dimensions|mr-system-read|mr-depth-system-read/);\n    const actions = ai.status === 'complete' ? find(/mr-report-options|mr-report-nextsteps|mr-ai-interpretation/) : find(/mr-run-action-board/);\n    const evidence = find(/mr-run-evidence|mr-evidence-status|mr-run-method|mr-meta-method/) || sections[sections.length - 1];\n    if (!sections[0] || !findings || !evidence) return '';\n    const tile = (number, title, content, target, footer, role) => '<a class=\"mr-overview-tile\" data-report-link-role=\"overview-' + role + '\" href=\"#' + (target || evidence).id + '\"><div class=\"mr-overview-title\"><span>' + number + '</span><h2>' + title + '</h2><span aria-hidden=\"true\">↗</span></div><div class=\"mr-overview-content\">' + content + '</div><div class=\"mr-overview-link\">' + footer + ' <span aria-hidden=\"true\">→</span></div></a>';\n    const summary = firstStr(interpretation.summary, m.centralFinding, m.execSummary, m.coverBody, m.primaryPattern);\n    const score = m.kind === 'run' ? m.score : m.scorePublished ? m.score : null;\n    const scoreLabel = m.kind === 'run' ? 'Diagnostic score' : firstStr(m.scoreLabel, 'Condition score');\n    const scoreBand = m.kind === 'run' ? firstStr(m.headlineBand) : firstStr(m.conditionBand);\n    const findingContent = '<div class=\"mr-overview-score\"><strong>' + esc(strictFinite(score) ? fmt1(score) : 'Unavailable') + '</strong>' + (strictFinite(score) ? '<span>/ 100</span>' : '') + '</div><p class=\"mr-overview-label\">' + esc(scoreLabel) + (scoreBand ? ' · ' + esc(scoreBand) : '') + '</p><p class=\"mr-overview-summary\">' + esc(summary || 'Review the recorded findings in the full report.') + '</p>';\n    const validated = threeBenefitPresentation(m);\n    let valueContent = '', valueTitle = 'Time and money', valueTarget = financial || evidence;\n    // Keep the overview on the central case. Low and high remain directly\n    // available in the complete, independently scaled money and time charts.\n    if (validated && validated.s.coverage.complete) {\n      const {s, input} = validated, totals = s.totals;\n      const baselineMoney = ['spendingReduction', 'spendingAvoidance'].reduce((sum, key) => sum + input[key].items.reduce((n, item) => n + item.baselineMonthlyUnits * item.unitCost * (item.endMonth - item.startMonth + 1), 0), 0);\n      const days = (Date.parse(input.capacity.measurementEnd) - Date.parse(input.capacity.measurementStart)) / 86400000;\n      const baselineHours = input.capacity.activities.reduce((n, item) => n + item.measuredHours * (input.horizonMonths * 365.25 / 12) / days, 0);\n      const flow = (kind, title, baseline, saved, format, note) => {\n        if (!Number.isFinite(baseline) || baseline <= 0 || baseline > 1e14 || !Number.isFinite(52 / baseline) || !strictFinite(saved) || saved < 0 || saved > baseline) return '<p class=\"mr-overview-note\">' + esc(title) + ': the baseline cannot be drawn reliably. Review the saved planning figures.</p>';\n        const share = saved / baseline, released = 52 * share, retained = 52 - released;\n        const color = kind === 'money' ? '#C9A227' : '#187783';\n        const ribbon = (start, end, width, fill) => width <= 0 ? '' : '<path d=\"M 8 ' + start + ' C 95 ' + start + ' 95 ' + end + ' 182 ' + end + ' L 182 ' + (end + width) + ' C 95 ' + (end + width) + ' 95 ' + (start + width) + ' 8 ' + (start + width) + ' Z\" fill=\"' + fill + '\" fill-opacity=\".4\"/>';\n        return '<div class=\"mr-overview-flow\" data-overview-flow=\"' + kind + '\" data-baseline=\"' + baseline + '\" data-released=\"' + saved + '\"><div class=\"mr-overview-flow-heading\"><span>' + title + '</span><strong>' + esc(format(saved)) + '</strong></div><svg viewBox=\"0 0 190 72\" aria-hidden=\"true\" focusable=\"false\">' + ribbon(6, 2, released, color) + ribbon(6 + released, 14 + released, retained, '#A9D0D4') + '<rect x=\"2\" y=\"6\" width=\"6\" height=\"52\" fill=\"#09383E\"/>' + (released > 0 ? '<rect x=\"182\" y=\"2\" width=\"6\" height=\"' + released + '\" fill=\"' + color + '\"/>' : '') + (retained > 0 ? '<rect x=\"182\" y=\"' + (14 + released) + '\" width=\"6\" height=\"' + retained + '\" fill=\"#A9D0D4\"/>' : '') + '</svg><div class=\"mr-overview-flow-caption\"><span>' + esc(format(baseline)) + ' baseline</span><span>Potential benefit + remaining</span></div><p class=\"mr-overview-note\">' + esc(note) + '</p></div>';\n      };\n      valueContent = '<p class=\"mr-overview-label\">' + (s.kind === 'early_planning_scenario' ? 'Early planning case · central' : 'Central planning case') + ' · ' + fmtWhole(input.horizonMonths) + ' months</p>' + flow('money', 'Spending reduced or avoided', baselineMoney, totals.existingSpendingReduction.central + totals.futureSpendingAvoidance.central, fmtMoney, fmtMoney(totals.existingSpendingReduction.central) + ' lower current spending; ' + fmtMoney(totals.futureSpendingAvoidance.central) + ' future spending avoided. Before costs.') + flow('time', 'Staff time freed', baselineHours, totals.grossPotentialHoursFreed.central, value => fmtWhole(value) + ' hours', fmtWhole(totals.potentialHoursFreed.central) + ' hours remain as staff capacity after time assigned to spending changes. Do not add hours to money.') + '<p class=\"mr-overview-note\">Directional estimates from recorded inputs and planning assumptions, not realized savings.</p>';\n    } else if (m.kind === 'run') {\n      valueTitle = 'What needs attention'; valueTarget = profile || findings;\n      valueContent = '<p class=\"mr-overview-summary\">' + esc(firstStr(m.centralFinding, m.bottomLine, m.execSummary, 'See which parts of the work need attention.')) + '</p><p>Compare the detailed findings and the responses behind them.</p><p class=\"mr-overview-note\">One person’s responses do not establish organization-wide time or financial savings.</p>';\n    } else {\n      valueContent = '<p class=\"mr-overview-summary\">' + (validated ? 'Some benefits have not been estimated.' : 'No complete planning scenario is recorded for this result.') + '</p><p>Review the available evidence and assumptions before estimating time or money.</p>';\n    }\n    const options = arr(interpretation.action_options).filter(item => typeof obj(item).action === 'string' && item.action.trim());\n    const recommendations = renderedAIRecommendations(ai);\n    const rows = options.length ? options.slice(0, 3).map(item => ({label: ({limited:'Limited change',moderate:'Moderate change',structural:'Structural change',aggressive:'Extensive change'})[item.intensity] || humanize(item.intensity || 'Change option'), text:item.action})) : recommendations.slice(0, 3).map((item, index) => ({label:'Next step ' + (index + 1), text:item.action}));\n    const actionContent = rows.length ? '<ol class=\"mr-overview-options\">' + rows.map(row => '<li><strong>' + esc(row.label) + '</strong><p>' + esc(row.text) + '</p></li>').join('') + '</ol><p class=\"mr-overview-note\">Open the full guidance for the rationale, prerequisites, risks and measures of success.</p>' : '<p class=\"mr-overview-summary\">' + (ai.status === 'complete' ? 'Review the interpretation before deciding what to change.' : 'The measured findings are available. Additional interpretation is not ready yet.') + '</p><p>Read the findings now and return for the additional guidance.</p>';\n    const counts = obj(obj(m.campaignEvidence).counts), people = counts.distinctParticipantsAcrossLenses, population = counts.declaredPopulation;\n    const recordedPeople = Number.isSafeInteger(people) && people > 0;\n    const populationKnown = Number.isSafeInteger(population) && population >= people;\n    const evidenceContent = m.kind === 'run' ? '<p class=\"mr-overview-summary\">One person’s view of the work</p><dl><div><dt>Diagnostic</dt><dd>' + esc(m.toolLabel) + '</dd></div><div><dt>Perspective</dt><dd>' + esc(firstStr(m.participantMode, 'Recorded in the result')) + '</dd></div><div><dt>Scope</dt><dd>' + esc(firstStr(m.scopeLabel, m.processName, 'See recorded context')) + '</dd></div></dl><p class=\"mr-overview-note\">Use the detailed responses and method to understand what this result supports.</p>' : '<div class=\"mr-overview-score\"><strong>' + esc(recordedPeople ? fmtWhole(people) : 'Unavailable') + '</strong>' + (recordedPeople && populationKnown ? '<span>of ' + fmtWhole(population) + '</span>' : '') + '</div><p class=\"mr-overview-label\">Recorded participants' + (recordedPeople && populationKnown ? ' / declared population' : '') + '</p><p>' + esc(firstStr(m.evidenceDescription, m.evidenceLabel, 'Review participation, scope and source checks.')) + '</p><p class=\"mr-overview-note\">People are counted once across lenses. Participation alone does not establish representative coverage.</p>';\n    const valueLink = validated && validated.s.coverage.complete ? 'Explore low, central and high cases' : financial ? 'Review planning inputs' : 'Explore the evidence';\n    return '<div class=\"mr-screen-only mr-report-overview\" aria-label=\"Report overview\"><p class=\"mr-overview-intro\">Your report at a glance. Select a tile to explore the full findings.</p><div class=\"mr-overview-grid\">' + tile('01', 'Overall findings', findingContent, findings, 'Read the findings', 'findings') + tile('02', valueTitle, valueContent, valueTarget, valueLink, 'value') + tile('03', options.length ? 'Change options' : 'Practical next steps', actionContent, actions || findings, 'Read the full guidance', 'actions') + tile('04', 'Evidence behind the result', evidenceContent, evidence, 'Review coverage and method', 'evidence') + '</div></div>';\n  }\n\n  function buildScreenReportControls(model, sections) {\n    const m = obj(model);\n    const ai = obj(m.aiReport);\n",
      "\n  const screenReportModels = new WeakMap();\n\n  function buildScreenReportControls(model, sections) {\n    const m = obj(model);\n    const ai = obj(m.aiReport);\n"
    ],
    [
      "    const evidence = find(/mr-run-evidence|mr-evidence-status/);\n    const actions = obj(m.aiReport).status === \"complete\" ? (find(/mr-report-options/) || find(/mr-report-nextsteps/) || find(/mr-ai-interpretation/)) : find(/mr-run-action-board|Evidence-proportionate actions|Conclusion and next step/);\n    const method = find(/mr-run-method|mr-meta-method|Method and limits/);\n    const shortcuts = [{ section: sections[0], label: \"Back to overview\", role: \"overview\" },\n      { section: decisionBrief, label: \"Decision brief\", role: \"financial-summary\" },\n      { section: profile, label: m.product === \"depth\" ? \"Distribution\" : m.product === \"cross_lens\" ? \"Compare lenses\" : \"Dimensions\" },\n      { section: evidence, label: \"Evidence\" }, { section: actions, label: interpretationOnly ? \"Interpretation\" : \"Actions\", role: \"guidance\" }, { section: method, label: \"Method & limits\" }].filter(item => item.section);\n",
      "    const evidence = find(/mr-run-evidence|mr-evidence-status/);\n    const actions = obj(m.aiReport).status === \"complete\" ? (find(/mr-report-options/) || find(/mr-report-nextsteps/) || find(/mr-ai-interpretation/)) : find(/mr-run-action-board|Evidence-proportionate actions|Conclusion and next step/);\n    const method = find(/mr-run-method|mr-meta-method|Method and limits/);\n    const shortcuts = [{ section: sections[0], label: \"Overview\" },\n      { section: decisionBrief, label: \"Decision brief\", role: \"financial-summary\" },\n      { section: profile, label: m.product === \"depth\" ? \"Distribution\" : m.product === \"cross_lens\" ? \"Compare lenses\" : \"Dimensions\" },\n      { section: evidence, label: \"Evidence\" }, { section: actions, label: interpretationOnly ? \"Interpretation\" : \"Actions\", role: \"guidance\" }, { section: method, label: \"Method & limits\" }].filter(item => item.section);\n"
    ],
    [
      "      : \"\";\n    const nextMove = actions ? '<div class=\"mr-screen-only mr-screen-next\"><div><span>Next step</span>' +\n      (firstAction ? '<p>' + esc(firstAction) + '</p>' : interpretationOnly ? '<p>Review the interpretation and its limits.</p>' : '<p>Review the suggested changes and their evidence before choosing a test.</p>') + '</div>' + link(actions, (interpretationOnly ? 'Review interpretation' : 'Explore actions') + ' <span aria-hidden=\"true\">→</span>', 'mr-screen-action', 'guidance') + '</div>' : '';\n    return { nav, nextMove, overview: buildReportOverview(m, sections) };\n  }\n\n  function refreshScreenReportAI(section, aiReport) {\n",
      "      : \"\";\n    const nextMove = actions ? '<div class=\"mr-screen-only mr-screen-next\"><div><span>Next step</span>' +\n      (firstAction ? '<p>' + esc(firstAction) + '</p>' : interpretationOnly ? '<p>Review the interpretation and its limits.</p>' : '<p>Review the suggested changes and their evidence before choosing a test.</p>') + '</div>' + link(actions, (interpretationOnly ? 'Review interpretation' : 'Explore actions') + ' <span aria-hidden=\"true\">→</span>', 'mr-screen-action', 'guidance') + '</div>' : '';\n    return { nav, nextMove };\n  }\n\n  function refreshScreenReportAI(section, aiReport) {\n"
    ],
    [
      "      node.id=section.id+'-guidance-'+index;\n      sections.push({id:node.id,classes:node.className,label:esc(node.querySelector('h3')?.textContent||'Actions')});\n    });\n    // Polling replaces the interpretation body. Restore its return link too,\n    // including on phones where the compact report navigation is not sticky.\n    if (sections[0] && !section.querySelector('.mr-section-back')) {\n      const back = document.createElement('div');\n      back.className = 'mr-screen-only mr-section-back';\n      const link = document.createElement('a');\n      link.href = '#' + sections[0].id;\n      link.dataset.reportLinkRole = 'overview';\n      link.textContent = '↑ Back to overview';\n      back.append(link);\n      section.prepend(back);\n    }\n    const controls = buildScreenReportControls(context.model, sections);\n    const update = (selector, html, parent, prepend = false) => {\n      const current = page.querySelector(selector);\n",
      "      node.id=section.id+'-guidance-'+index;\n      sections.push({id:node.id,classes:node.className,label:esc(node.querySelector('h3')?.textContent||'Actions')});\n    });\n    const controls = buildScreenReportControls(context.model, sections);\n    const update = (selector, html, parent, prepend = false) => {\n      const current = page.querySelector(selector);\n"
    ],
    [
      "    };\n    update('.mr-screen-nav', controls.nav, page, true);\n    update('.mr-screen-next', controls.nextMove, page.querySelector('.mr-cover-white'));\n    update('.mr-report-overview', controls.overview, page.querySelector('.mr-cover-white'));\n  }\n\n  // Screen navigation is a presentation of the existing report body. It never\n",
      "    };\n    update('.mr-screen-nav', controls.nav, page, true);\n    update('.mr-screen-next', controls.nextMove, page.querySelector('.mr-cover-white'));\n  }\n\n  // Screen navigation is a presentation of the existing report body. It never\n"
    ],
    [
      "      const label = classes === \"mr-cover\" ? \"Overview\" : heading ? heading[1].replace(/<[^>]+>/g, \"\").replace(/^\\d+\\.\\s*/, \"\") : \"Report section\";\n      sections.push({ id, classes, label });\n      // Preserve aria-live and any other section attributes in every state.\n      return tag.replace(/\\s+id=\"[^\"]*\"/, '').replace(/^<section\\b/, '<section id=\"' + id + '\"') + (sections.length > 1 ? '<div class=\"mr-screen-only mr-section-back\"><a data-report-link-role=\"overview\" href=\"#' + sections[0].id + '\">↑ Back to overview</a></div>' : '');\n    });\n    let guidanceIndex=0;\n    const guidanceSections=[];\n",
      "      const label = classes === \"mr-cover\" ? \"Overview\" : heading ? heading[1].replace(/<[^>]+>/g, \"\").replace(/^\\d+\\.\\s*/, \"\") : \"Report section\";\n      sections.push({ id, classes, label });\n      // Preserve aria-live and any other section attributes in every state.\n      return tag.replace(/\\s+id=\"[^\"]*\"/, '').replace(/^<section\\b/, '<section id=\"' + id + '\"');\n    });\n    let guidanceIndex=0;\n    const guidanceSections=[];\n"
    ],
    [
      "    });\n    const interpretationIndex=sections.findIndex(s=>s.classes.includes('mr-ai-interpretation'));\n    if(interpretationIndex>=0)sections.splice(interpretationIndex+1,0,...guidanceSections);\n    const { nav, nextMove, overview } = buildScreenReportControls(m, sections);\n    const boundary = m.kind === \"run\" && m.footnote ? '<div class=\"mr-screen-only mr-screen-boundary\"><strong>' + esc(firstStr(m.evidenceBand, \"Single-run evidence\")) + '</strong><p>' + esc(m.footnote) + '</p></div>' : '';\n    // Insert alongside the score; the existing full interpretation boundary,\n    // method, provenance, and next-decision sections remain in the body.\n",
      "    });\n    const interpretationIndex=sections.findIndex(s=>s.classes.includes('mr-ai-interpretation'));\n    if(interpretationIndex>=0)sections.splice(interpretationIndex+1,0,...guidanceSections);\n    const { nav, nextMove } = buildScreenReportControls(m, sections);\n    const boundary = m.kind === \"run\" && m.footnote ? '<div class=\"mr-screen-only mr-screen-boundary\"><strong>' + esc(firstStr(m.evidenceBand, \"Single-run evidence\")) + '</strong><p>' + esc(m.footnote) + '</p></div>' : '';\n    // Insert alongside the score; the existing full interpretation boundary,\n    // method, provenance, and next-decision sections remain in the body.\n"
    ],
    [
      "    // safe if a future cover template changes its closing structure.\n    const coverEnd = body.indexOf('</section>');\n    const cover = coverEnd < 0 ? '' : body.slice(0, coverEnd + '</section>'.length);\n    if (cover.endsWith(coverClose)) body = cover.slice(0, -coverClose.length) + overview + boundary + nextMove + coverClose + body.slice(cover.length);\n    if (overview) body = body.replace('class=\"mr-cover-white\"', 'class=\"mr-cover-white mr-has-overview\"');\n    // SVG definition IDs share the same per-mount namespace as the sections.\n    body = body.replace(/id=\"mr-system-gradient\"/g, 'id=\"' + prefix + '-system-gradient\"').replace(/url\\(#mr-system-gradient\\)/g, 'url(#' + prefix + '-system-gradient)');\n    // Native case radios and their labels must remain independent when the\n",
      "    // safe if a future cover template changes its closing structure.\n    const coverEnd = body.indexOf('</section>');\n    const cover = coverEnd < 0 ? '' : body.slice(0, coverEnd + '</section>'.length);\n    if (cover.endsWith(coverClose)) body = cover.slice(0, -coverClose.length) + boundary + nextMove + coverClose + body.slice(cover.length);\n    // SVG definition IDs share the same per-mount namespace as the sections.\n    body = body.replace(/id=\"mr-system-gradient\"/g, 'id=\"' + prefix + '-system-gradient\"').replace(/url\\(#mr-system-gradient\\)/g, 'url(#' + prefix + '-system-gradient)');\n    // Native case radios and their labels must remain independent when the\n"
    ],
    [
      "      .mr-report .mr-cover-sub{max-width:90ch;font-size:.86rem!important;line-height:1.5!important;margin-top:10px!important;color:#CEE1E3!important}\n      .mr-report .mr-cover-stripe{height:2px;background:#15949F}\n      .mr-report .mr-cover-white{padding:22px 26px 24px}\n      .mr-report .mr-has-overview>.mr-cover-score-row,.mr-report .mr-has-overview>.mr-cover-body,.mr-report .mr-has-overview>.mr-cover-kicker,.mr-report .mr-has-overview>.mr-screen-next{display:none}\n      .mr-report .mr-has-overview>.mr-cover-meta{margin-top:0;padding-top:0;border-top:0}\n      .mr-overview-intro{color:#53676E;font-size:.88rem!important;margin:20px 0 16px!important}\n      .mr-section-back{display:flex;justify-content:flex-end;margin:0 0 12px}.mr-section-back a{display:inline-flex;align-items:center;min-height:44px;padding:8px 0;color:#187783;font-size:.8rem;text-decoration:none}.mr-section-back a:focus-visible{outline:3px solid #C9A227;outline-offset:3px}\n      .mr-overview-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px;align-items:stretch}\n      .mr-report a.mr-overview-tile{display:flex;flex-direction:column;min-width:0;color:#09383E;text-decoration:none;background:#fff;border:1px solid #CCDCDD;border-radius:9px;overflow:hidden;box-shadow:0 3px 10px #09383E08;transition:box-shadow .16s ease,border-color .16s ease}\n      .mr-report a.mr-overview-tile:hover{color:#09383E;border-color:#187783;box-shadow:0 5px 18px #09383E12}\n      .mr-report a.mr-overview-tile:focus-visible{outline:3px solid #C9A227;outline-offset:4px}\n      .mr-overview-title{display:flex;align-items:center;gap:12px;min-height:58px;padding:15px 20px;background:#187783;color:#fff}\n      .mr-overview-title>span:first-child{font-size:.7rem;letter-spacing:.05em;color:#D5EBED}\n      .mr-report .mr-overview-title h2{flex:1;margin:0!important;font-size:1rem!important;line-height:1.35!important;color:#fff!important;letter-spacing:0!important}\n      .mr-overview-content{padding:22px;flex:1;min-width:0;overflow-wrap:anywhere}\n      .mr-report .mr-overview-content p{font-size:.88rem;line-height:1.55;margin:12px 0}\n      .mr-overview-score{display:flex;align-items:baseline;gap:10px;flex-wrap:wrap}\n      .mr-overview-score>strong{font-size:2.65rem;font-weight:600;line-height:1.12;letter-spacing:-.025em}\n      .mr-overview-score>span{color:#53676E;font-size:1rem}\n      .mr-report .mr-overview-label{color:#53676E;font-size:.75rem!important;margin:6px 0 16px!important}\n      .mr-report .mr-overview-summary{font-size:1rem!important;line-height:1.55!important}\n      .mr-report .mr-overview-note{font-size:.75rem!important;line-height:1.5!important;color:#53676E}\n      .mr-overview-link{border-top:1px solid #E3EAEA;padding:14px 22px;color:#187783;font-size:.82rem;font-weight:600;min-height:48px;display:flex;justify-content:space-between;gap:14px;align-items:center}\n      .mr-overview-options{margin:0;padding:0;list-style:none}\n      .mr-overview-options>li+li{border-top:1px solid #E3EAEA;margin-top:14px;padding-top:14px}\n      .mr-overview-options strong{font-size:.82rem;color:#187783}\n      .mr-report .mr-overview-options p{margin:5px 0 0}\n      .mr-overview-content dl{margin:20px 0 0;font-size:.85rem}\n      .mr-overview-content dl>div{display:grid;grid-template-columns:minmax(80px,1fr) minmax(0,2fr);gap:10px;padding:9px 0;border-top:1px solid #E3EAEA}\n      .mr-overview-content dt{color:#53676E}.mr-overview-content dd{margin:0}\n      .mr-overview-flow{border-top:1px solid #E3EAEA;padding-top:12px;margin-top:12px}\n      .mr-overview-flow-heading{display:flex;justify-content:space-between;gap:14px;font-size:.8rem;align-items:baseline;flex-wrap:wrap}\n      .mr-overview-flow-heading strong{font-size:1.1rem;color:#09383E}\n      .mr-overview-flow svg{display:block;width:100%;height:76px;margin:7px 0}\n      .mr-overview-flow-caption{display:flex;justify-content:space-between;gap:18px;font-size:.68rem;color:#53676E}\n      .mr-overview-flow-caption>span{max-width:48%}.mr-overview-flow-caption>span+span{text-align:right}\n      @media(max-width:700px){.mr-overview-grid{grid-template-columns:1fr;gap:16px}.mr-overview-content{padding:18px}.mr-overview-title{padding:14px 18px}.mr-overview-link{padding:14px 18px}.mr-overview-score>strong{font-size:2.3rem}}\n      @media(prefers-reduced-motion:reduce){.mr-report a.mr-overview-tile{transition:none}}\n      .mr-report .mr-cover-kicker{margin-bottom:12px!important;font-size:.61rem!important;letter-spacing:.12em}\n      .mr-report .mr-cover-score:not(.mr-cover-score-status){font-size:3.6rem;line-height:.95;letter-spacing:-.055em;color:#07343A}\n      .mr-report .mr-cover-score-label{color:#087F8C;letter-spacing:.08em;font-size:.7rem}\n",
      "      .mr-report .mr-cover-sub{max-width:90ch;font-size:.86rem!important;line-height:1.5!important;margin-top:10px!important;color:#CEE1E3!important}\n      .mr-report .mr-cover-stripe{height:2px;background:#15949F}\n      .mr-report .mr-cover-white{padding:22px 26px 24px}\n      .mr-report .mr-cover-kicker{margin-bottom:12px!important;font-size:.61rem!important;letter-spacing:.12em}\n      .mr-report .mr-cover-score:not(.mr-cover-score-status){font-size:3.6rem;line-height:.95;letter-spacing:-.055em;color:#07343A}\n      .mr-report .mr-cover-score-label{color:#087F8C;letter-spacing:.08em;font-size:.7rem}\n"
    ]
  ]
}
