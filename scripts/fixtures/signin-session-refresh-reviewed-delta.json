{
  "currentSha": "8cf251e2e0534f44bb34fdb4b8b25fd61b82ffead21b5d821e7d076ed9761399",
  "priorSha": "f0240d14bb0b707d64fa2d5f422fb6c82f7d82e91ed0a4774b2f30dd9832bd63",
  "replacements": [
    [
      "    let acceptanceCheckStarted = false;\n    let pendingSession = null;\n    let currentDocuments = null;\n    let legalSubmissionInProgress = false;\n    let legalAuthRevision = 0;\n    function clearLegalSession(){\n      legalAuthRevision++;\n      pendingSession = null;\n      currentDocuments = null;\n      acceptanceCheckStarted = false;\n      legalSubmissionInProgress = false;\n      ui.legalAgree.checked = false;\n      ui.legalAgree.disabled = false;\n      ui.legalDecline.disabled = false;\n      ui.legalSubmit.disabled = true;\n      ui.legalSubmit.textContent = \"Agree and continue\";\n      ui.legalAcceptance.classList.remove(\"show\");\n      ui.google.hidden = false;\n      document.querySelector(\".divider\").hidden = false;\n      ui.emailForm.hidden = false;\n    }\n    function assertLegalIdentity(userId, revision){\n      if (revision !== legalAuthRevision || pendingSession?.user?.id !== userId) throw new Error(\"legal_session_changed\");\n    }\n    async function currentLegalSession(userId, revision, refresh=false){\n      // Read SDK-owned state, never refresh with the old page-load token.\n      // getSession refreshes an expired session when necessary.\n      let result = await supabase.auth.getSession();\n      assertLegalIdentity(userId, revision);\n      if (result.error || !result.data?.session?.access_token || result.data.session.user?.id !== userId) {\n        clearLegalSession();\n        throw new Error(\"legal_session_changed\");\n      }\n      if (refresh) {\n        result = await supabase.auth.refreshSession();\n        assertLegalIdentity(userId, revision);\n        if (result.error || !result.data?.session?.access_token || result.data.session.user?.id !== userId) {\n          clearLegalSession();\n          throw new Error(\"legal_session_changed\");\n        }\n      }\n      return result.data.session;\n    }\n    function legalDocumentPath(kind, version){\n      // Exact archived editions in legal-document-manifest.json, not a suffix rule.\n      const versions = {\n",
      "    let acceptanceCheckStarted = false;\n    let pendingSession = null;\n    let currentDocuments = null;\n    function legalDocumentPath(kind, version){\n      // Exact archived editions in legal-document-manifest.json, not a suffix rule.\n      const versions = {\n"
    ],
    [
      "      acceptanceCheckStarted = true;\n      clearPendingOtp();\n      pendingSession = session;\n      const revision = legalAuthRevision;\n      const context = acceptanceContext();\n      const query = new URLSearchParams({ source: context.source });\n      if (context.inviteToken) query.set(\"invite_token\", context.inviteToken);\n",
      "      acceptanceCheckStarted = true;\n      clearPendingOtp();\n      pendingSession = session;\n      const context = acceptanceContext();\n      const query = new URLSearchParams({ source: context.source });\n      if (context.inviteToken) query.set(\"invite_token\", context.inviteToken);\n"
    ],
    [
      "        // The free diagnostic returns in this same tab. Bind its private\n        // recovery record to a server-verified identity before continuing.\n        await window.MondermanDVJourneyRecovery?.bindAuthenticatedReturn(supabase, nextTarget, session.user.id);\n        assertLegalIdentity(session.user.id, revision);\n        const response = await fetch(`${API_BASE}/api/legal/acceptance/status?${query.toString()}`, {\n          headers: { authorization: `Bearer ${session.access_token}` }\n        });\n        const result = await response.json().catch(() => ({}));\n        assertLegalIdentity(session.user.id, revision);\n        if (!response.ok || !result.ok) {\n          const statusError = new Error(result.error || \"legal_acceptance_status_failed\");\n          statusError.code = result.error || \"legal_acceptance_status_failed\";\n",
      "        // The free diagnostic returns in this same tab. Bind its private\n        // recovery record to a server-verified identity before continuing.\n        await window.MondermanDVJourneyRecovery?.bindAuthenticatedReturn(supabase, nextTarget, session.user.id);\n        const response = await fetch(`${API_BASE}/api/legal/acceptance/status?${query.toString()}`, {\n          headers: { authorization: `Bearer ${session.access_token}` }\n        });\n        const result = await response.json().catch(() => ({}));\n        if (!response.ok || !result.ok) {\n          const statusError = new Error(result.error || \"legal_acceptance_status_failed\");\n          statusError.code = result.error || \"legal_acceptance_status_failed\";\n"
    ],
    [
      "        if (!result.requiresAcceptance){ forwardOn(); return; }\n        setExactLegalDocumentLinks(result);\n        currentDocuments = result;\n        ui.legalAgree.checked = false;\n        ui.legalSubmit.disabled = true;\n        clearTimeout(revealTimer);\n        ui.sessionCheck.classList.add(\"hidden\");\n        ui.google.hidden = true;\n",
      "        if (!result.requiresAcceptance){ forwardOn(); return; }\n        setExactLegalDocumentLinks(result);\n        currentDocuments = result;\n        clearTimeout(revealTimer);\n        ui.sessionCheck.classList.add(\"hidden\");\n        ui.google.hidden = true;\n"
    ],
    [
      "        ui.legalAgree.focus();\n        setStatus(\"\");\n      }catch(error){\n        if (revision !== legalAuthRevision) return;\n        acceptanceCheckStarted = false;\n        clearTimeout(revealTimer);\n        ui.sessionCheck.classList.add(\"hidden\");\n",
      "        ui.legalAgree.focus();\n        setStatus(\"\");\n      }catch(error){\n        acceptanceCheckStarted = false;\n        clearTimeout(revealTimer);\n        ui.sessionCheck.classList.add(\"hidden\");\n"
    ],
    [
      "    });\n\n    ui.legalAgree.addEventListener(\"change\", () => {\n      ui.legalSubmit.disabled = legalSubmissionInProgress || !currentDocuments || !ui.legalAgree.checked;\n    });\n    ui.legalDecline.addEventListener(\"click\", async () => {\n      if (legalSubmissionInProgress) return;\n      ui.legalAgree.disabled = true;\n      ui.legalSubmit.disabled = true;\n      ui.legalDecline.disabled = true;\n",
      "    });\n\n    ui.legalAgree.addEventListener(\"change\", () => {\n      ui.legalSubmit.disabled = !ui.legalAgree.checked;\n    });\n    ui.legalDecline.addEventListener(\"click\", async () => {\n      ui.legalAgree.disabled = true;\n      ui.legalSubmit.disabled = true;\n      ui.legalDecline.disabled = true;\n"
    ],
    [
      "      }\n    });\n    ui.legalSubmit.addEventListener(\"click\", async () => {\n      if (forwarded || legalSubmissionInProgress || !ui.legalAgree.checked || !pendingSession?.user?.id || !currentDocuments) return;\n      const context = acceptanceContext();\n      const userId = pendingSession.user.id, revision = legalAuthRevision;\n      const agreedDocuments = { termsVersion: currentDocuments.termsVersion, privacyNoticeVersion: currentDocuments.privacyNoticeVersion };\n      const body = JSON.stringify({\n        agreed: true,\n        source: context.source,\n        invite_token: context.inviteToken || undefined,\n        terms_version: agreedDocuments.termsVersion,\n        privacy_notice_version: agreedDocuments.privacyNoticeVersion\n      });\n      const query = new URLSearchParams({ source: context.source });\n      if (context.inviteToken) query.set(\"invite_token\", context.inviteToken);\n      legalSubmissionInProgress = true;\n      ui.legalAgree.disabled = true;\n      ui.legalDecline.disabled = true;\n      ui.legalSubmit.disabled = true;\n      ui.legalSubmit.textContent = \"Recording…\";\n      try{\n        // At most one explicit refresh/retry, and only after a real HTTP 401.\n        // Every attempt rechecks the editions; a changed edition needs a new click.\n        for (let attempt = 0; attempt < 2; attempt++) {\n          let session = await currentLegalSession(userId, revision, attempt === 1);\n          assertLegalIdentity(userId, revision);\n          const statusResponse = await fetch(`${API_BASE}/api/legal/acceptance/status?${query.toString()}`, {\n            headers: { authorization: `Bearer ${session.access_token}` }\n          });\n          const status = await statusResponse.json().catch(() => ({}));\n          assertLegalIdentity(userId, revision);\n          if (statusResponse.status === 401 && attempt === 0) continue;\n          if (!statusResponse.ok || !status.ok) throw new Error(status.error || \"legal_acceptance_status_failed\");\n          if (status.termsVersion !== agreedDocuments.termsVersion || status.privacyNoticeVersion !== agreedDocuments.privacyNoticeVersion) {\n            ui.legalAgree.checked = false;\n            currentDocuments = null;\n            setExactLegalDocumentLinks(status);\n            currentDocuments = status;\n            throw new Error(\"legal_documents_changed\");\n          }\n          session = await currentLegalSession(userId, revision);\n          assertLegalIdentity(userId, revision);\n          const response = await fetch(`${API_BASE}/api/legal/acceptance`, {\n            method: \"POST\",\n            headers: { \"content-type\": \"application/json\", authorization: `Bearer ${session.access_token}` },\n            body\n          });\n          const result = await response.json().catch(() => ({}));\n          assertLegalIdentity(userId, revision);\n          if (response.status === 401 && attempt === 0) continue;\n          if (!response.ok || !result.ok) {\n            if (result.error === \"legal_document_version_changed\") { ui.legalAgree.checked = false; currentDocuments = null; }\n            throw new Error(result.error || \"legal_acceptance_failed\");\n          }\n          forwardOn();\n          return;\n        }\n      }catch(error){\n        if (revision !== legalAuthRevision) {\n          if (!pendingSession) setStatus(\"Your sign-in changed or expired. Sign in again and review the Terms and Privacy Notice before agreeing.\", \"error\");\n          return;\n        }\n        setStatus(error?.message === \"legal_session_changed\"\n          ? \"Your sign-in changed or expired. Sign in again and review the Terms and Privacy Notice before agreeing.\"\n          : [\"legal_documents_changed\", \"legal_document_version_changed\", \"invalid_legal_document_version\"].includes(error?.message)\n          ? \"The Terms or Privacy Notice changed. Reload this page and review the updated documents before agreeing again.\"\n          : invitationMode\n          ? \"We couldn’t record your agreement, so the invitation was not accepted. Please try again.\"\n          : \"We couldn’t record your agreement. Nothing was activated; please try again.\", \"error\");\n        console.warn(\"Legal acceptance error:\", error?.message || error);\n      }finally{\n        if (revision === legalAuthRevision) {\n          legalSubmissionInProgress = false;\n          ui.legalAgree.disabled = false;\n          ui.legalDecline.disabled = false;\n          ui.legalSubmit.disabled = forwarded || !currentDocuments || !ui.legalAgree.checked;\n          ui.legalSubmit.textContent = \"Agree and continue\";\n        }\n      }\n    });\n\n",
      "      }\n    });\n    ui.legalSubmit.addEventListener(\"click\", async () => {\n      if (!ui.legalAgree.checked || !pendingSession || !currentDocuments) return;\n      const context = acceptanceContext();\n      ui.legalSubmit.disabled = true;\n      ui.legalSubmit.textContent = \"Recording…\";\n      try{\n        const response = await fetch(`${API_BASE}/api/legal/acceptance`, {\n          method: \"POST\",\n          headers: { \"content-type\": \"application/json\", authorization: `Bearer ${pendingSession.access_token}` },\n          body: JSON.stringify({\n            agreed: true,\n            source: context.source,\n            invite_token: context.inviteToken || undefined,\n            terms_version: currentDocuments.termsVersion,\n            privacy_notice_version: currentDocuments.privacyNoticeVersion\n          })\n        });\n        const result = await response.json().catch(() => ({}));\n        if (!response.ok || !result.ok) throw new Error(result.error || \"legal_acceptance_failed\");\n        forwardOn();\n      }catch(error){\n        ui.legalSubmit.disabled = false;\n        ui.legalSubmit.textContent = \"Agree and continue\";\n        setStatus(invitationMode\n          ? \"We couldn’t record your agreement, so the invitation was not accepted. Please try again.\"\n          : \"We couldn’t record your agreement. Nothing was activated; please try again.\", \"error\");\n        console.warn(\"Legal acceptance error:\", error?.message || error);\n      }\n    });\n\n"
    ],
    [
      "\n    // Forward the instant Supabase finishes establishing the session.\n    supabase.auth.onAuthStateChange((event, session) => {\n      if (event === \"SIGNED_OUT\" || (pendingSession && session?.user?.id !== pendingSession.user?.id)) {\n        clearLegalSession();\n        if (event === \"SIGNED_OUT\") window.MondermanDVJourneyRecovery?.clearPending();\n      }\n      // Run the verified-user lookup outside Supabase's auth-event callback.\n      const revision = legalAuthRevision;\n      if (session?.user) setTimeout(() => { if (revision === legalAuthRevision) void continueAfterAuth(session); }, 0);\n    });\n\n    // Safety net: never trap the user on the spinner. Wait LONGER if we're\n",
      "\n    // Forward the instant Supabase finishes establishing the session.\n    supabase.auth.onAuthStateChange((event, session) => {\n      if (event === \"SIGNED_OUT\") window.MondermanDVJourneyRecovery?.clearPending();\n      // Run the verified-user lookup outside Supabase's auth-event callback.\n      if (session?.user) setTimeout(() => { void continueAfterAuth(session); }, 0);\n    });\n\n    // Safety net: never trap the user on the spinner. Wait LONGER if we're\n"
    ]
  ]
}
